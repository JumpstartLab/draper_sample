<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= stylesheet_link_tag "styles" %>
    <%= javascript_include_tag :defaults %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
  <p style="color: green"><%= flash[:notice] %></p>

  <div id="account">
    <% if current_user %>
      <span>Welcome, <%= current_user %>!</span>
      <%= link_to "logout", logout_path %>
    <% else %>
      Login Here
    <% end %>
  </div>

  <div class="wrapper">
      <div class="header">
        <h1><%= link_to "FoodWorks", products_path %></h1>
        <p>Your Online Grocery</p>
      </div>

      <div class="sidebar">
        (sidebar)  
      </div>

      <div class="main">
        <%= yield %>
      </div>

      <div class="footer">
        FoodWorks Online Grocery<br/>
        A Rails Jumpstart Project
      </div>
      
      <% if Rails.env.development? %>
        <%= debug params %>
      <% end %>
  </div>
  </body>
</html>
